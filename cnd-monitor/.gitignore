node_modules/

# `calibracao/` NAO entra aqui de proposito: o inventario e a captura de tela
# gerados por `npm run calibrar` sao feitos para serem commitados e revisados,
# e sao o insumo para ajustar os seletores em src/receitas/.
